{
  "name": "iepp-web",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio estático de la Iglesia Evangélica de los Peregrinos del Perú",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "check": "astro check",
    "build": "astro build && node scripts/check-static.mjs",
    "preview": "astro preview --host 127.0.0.1"
  },
  "type": "module",
  "dependencies": {
    "astro": "7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "6.0.3"
  }
}
